{
  "id": "cs-agent-pro",
  "name": "CS Agent Pro",
  "description": "Turns an OpenClaw agent into a professional, knowledge-grounded customer-service agent. Ships an onboarding-gate hook (proactive setup) and a cs-ops hook (conversation logging, KB-gap capture, and real out-of-band escalation delivery).",
  "version": "1.4.0",
  "activation": {
    "onStartup": false
  },
  "hooks": [
    {
      "id": "onboarding-gate",
      "path": "hooks/onboarding-gate",
      "events": ["command:new", "message:received"]
    },
    {
      "id": "cs-ops",
      "path": "hooks/cs-ops",
      "events": ["message:received", "message:sent"]
    }
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" }
    }
  }
}
